The Java source code is the .java file with the text that is written by the developer.
The ByteCode is the set of instructions that is made by the javac compiler from the java source code.
Finally, the JVM is OS specific, however its function is to run java byte code.
The idea of java is to streamline development by allowing any computer to run compiled code, regardless of what OS compiled that byte code.
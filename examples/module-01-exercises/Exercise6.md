the Main method gets pushed onto the stack, then add gets pushed onto the stack along with its parameters.
THen, it gets popped off the stack with its return value. 
Then greet is added to the stack and the same process happens again.
<article aria-label={`${name} (${customerId})`}>
<h3 id={`customer-${customerId}`}>{name}</h3>
<StatusBadge status={status} /> {/* renders TEXT, not color alone */}
<button type="button" onClick={() => onSelect(customerId)}>
View
</button>
</article>



status colors need shape and text too to be accessible to those with a screen reader (tts)

prefer headers and buttons over divs with onClick

tab order for elements on the page.
# Backend CORS checklist (sibling Spring app)

TODO: allow `http://localhost:5173` (or your Vite port) for timed-path demos.
TODO: do not use `*` with credentials.

- Drafted a backend to be used within lab35.
- cd lab35-crm/crm-api and then run mvn spring-boot:run
# TODO: lab-only teaser — e.g. logging.level.root: DEBUG
# Do not put real secrets here (Lab 26 deepens profiles).
erver:
  port: 8080
logging:
  level:
    com.northstar.crm: DEBUG
management:
  endpoint:
    health:
      show-details: always
spring:
  datasource:
    # TODO: H2 URL for tests
    url:  jdbc:h2:mem:lab26test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
northstar:
  integration:
    connect-timeout-ms: 100

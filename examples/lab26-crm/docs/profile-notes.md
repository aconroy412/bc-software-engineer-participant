# Lab 26 — Profile / precedence notes

1. TODO: which source won in your override experiment (CLI > env > profile YAML > base)?
CLI = 1234 Env = 9999 base = 100. CLI won
2. TODO: evidence that prod refuses missing secrets
does not compile in step 4
